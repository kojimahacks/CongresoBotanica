module.exports = function(answers) {
  answers.env = {};

  answers.environments.forEach(function(elem) {
    answers.env[elem] = true;
  });

  answers.esversion = parseInt(answers.esversion);

  return answers;
};
